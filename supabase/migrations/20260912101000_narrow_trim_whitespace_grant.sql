revoke execute on function internal.trim_whitespace(text) from authenticated;

comment on function internal.trim_whitespace(text) is
  '[CLIENT coupon request 2026-09-12] Removes whitespace from both ends of a value: ASCII whitespace (tab, line feed, vertical tab, form feed, carriage return, space), next line, the no-break and Unicode space separators, the zero-width spaces, joiners and marks, the line and paragraph separators and the byte-order mark. That is everything JavaScript''s String.prototype.trim removes plus the invisible zero-width characters, so a value the console has already trimmed is only ever shortened further by characters nobody can see. Interior characters are never touched. Executable by the owner and the service role only. The promo_codes_batch_name_shaped check constraint calls it, and PostgreSQL checks EXECUTE on a function inside a constraint against the role performing the write. The service role holds INSERT and UPDATE on promo_codes, so it keeps the grant. The signed-in role holds only SELECT and writes coupons through definer functions, which run as the owner, so it needs none. 20260912101000 withdrew the grant 20260912100000 gave it, which keeps the reachable surface of the internal schema pinned in supabase/tests/hardening.sql unchanged.';
