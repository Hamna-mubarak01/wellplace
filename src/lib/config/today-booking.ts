export const TODAY_BOOKING_COPY = {
  closingIn: "Book your slot now",
  fullyBooked: "Today is fully booked",
  closed: "Today's bookings are closed",
  otherDays: "See other days",
  hoursShort: "h",
  minutesShort: "min",
} as const;

export const TODAY_BOOKING_CACHE_MS = 30_000;
export const TODAY_BOOKING_REFRESH_MS = 120_000;
export const TODAY_BOOKING_TICK_MS = 1_000;
export const TODAY_BOOKING_REDUCED_TICK_MS = 30_000;
